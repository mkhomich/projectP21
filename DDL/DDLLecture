CREATE TABLE lecture (
  course BIGINT REFERENCES course (id),
  id BIGINT PRIMARY KEY,
  homework BIGINT REFERENCES homework (id),
  teacher BIGINT REFERENCES "user" (id),
  lectureNumber INT,
  topic TEXT,
  payload TEXT
);